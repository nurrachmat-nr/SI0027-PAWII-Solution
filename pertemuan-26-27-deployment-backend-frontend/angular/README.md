# Solusi Referensi - Angular Standalone + Konfigurasi Deployment

Lihat README utama pada folder [`pertemuan-26-27-deployment-backend-frontend`](../README.md) untuk penjelasan lengkap deployment, cara menjalankan backend, dan kaitannya dengan Tugas Project Mandiri.

Kode identik dengan Pertemuan 23-24-25, ditambah `vercel.json` & `netlify.toml` (output direktori: `dist/project-solution/browser`). Jalankan cepat secara lokal:

```bash
npm install
npm start
```
